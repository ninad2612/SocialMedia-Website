{
    "builds": [{
        "src": "usermodel_withextra/wsgi.py",
        "use": "@vercel/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "usermodel_withextra/wsgi.py"
        }
    ]
}